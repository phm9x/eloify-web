import { Placeholder } from "@/ui/components/Placeholder";

export function LogGame() {
  return (
    <Placeholder
      title="Log Game"
      note="1v1 / 2v2 toggle, player name fields, live projected-delta preview, then submit to the sheet."
    />
  );
}
