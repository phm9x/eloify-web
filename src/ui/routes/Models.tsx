import { Placeholder } from "@/ui/components/Placeholder";

export function Models() {
  return (
    <Placeholder
      title="Models"
      note="The available rating models (mov, elo, provisional, share) and which is active."
    />
  );
}
